;; Load geben
;;(setq load-path (cons "~/.emacs.d/geben-0.26" load-path))
(;;autoload 'geben "geben" "PHP Debugger on Emacs" t)

;; Global keys for geben
;;(global-set-key (kbd "C-x RET RET") 'geben)
;;(global-set-key (kbd "C-x #") 'geben-end)
